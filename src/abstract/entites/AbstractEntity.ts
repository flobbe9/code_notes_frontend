/**
 * Interface representing an abstract class that is inherited by any entity.
 * 
 * @since 0.0.1
 */
export interface AbstractEntity {

    id?: number | null;

    created?: string;

    updated?: string;
}