/**
 * Describes an HTMLElement that can have a cursor inside which is movable by the `moveCursor` function.
 * 
 * @since 1.1.0
 */
export type MovableCursorElement = HTMLTextAreaElement | HTMLDivElement | HTMLInputElement;
