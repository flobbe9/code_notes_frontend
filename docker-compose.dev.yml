services:
  frontend:
    image: dev_${API_NAME}
    container_name: frontend
    build:
      context: .
      args:
        NODE_VERSION: ${NODE_VERSION}
        APP_ENV: development
        BACKEND_PORT: 1111
    # NOTE: this does not override application .env vars
    environment:
      TZ: ${TZ}
    ports:
      - 1112:1112
    healthcheck:
      test: wget --no-verbose --no-check-certificate --tries=1 --spider http://host.docker.internal:3000 || exit 1
      interval: 5s
      timeout: 10s
      retries: 5
    restart: on-failure
    volumes:
      - ./.nginx.local/nginx.local.conf:/etc/nginx/conf.d/default.conf